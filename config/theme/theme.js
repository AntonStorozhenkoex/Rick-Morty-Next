import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        cardBgColor: "#0C7D85",
        cardBorderColor: "#CAE26A",
        notResultColor: "#fff",
        inputBgColor: "#407772"
    },
});
